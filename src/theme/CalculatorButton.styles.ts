import { Dimensions, StyleSheet } from "react-native";

export const buttonArea = 85

export default StyleSheet.create({
    calculatorButton: {
        height: buttonArea,
        width: buttonArea,
        borderRadius: 100,
        margin: 2,
        alignItems: "center",
        justifyContent: 'center'
    },
})

