import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        borderRadius: 60,
        alignItems: 'center',
        justifyContent: 'center'   
    }
})