import {StyleSheet} from 'react-native';
import {COLORS} from '../../../themes';

export const calculatorLastValueStyles = StyleSheet.create({
  text: {
    fontSize: 24,
    color: COLORS.grayLight,
  },
});
