import {StyleSheet} from 'react-native';
import {COLORS} from '../../../themes';

export const calculatorPreviewStyles = StyleSheet.create({
  text: {
    fontSize: 56,
    color: COLORS.white,
  },
});
